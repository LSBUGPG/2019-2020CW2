# **COMPONENT 3 - Disappearing Objects - Journal**

## 25/04/2020

On this third component we set off an object, this related to the pick up collecatbles on the 1 brief.

This type of switch off can be use in other kind of components as it only need to `gameObject.SetActive(false)´ after an `if()`.